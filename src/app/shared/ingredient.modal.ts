export class Ingredient {
  constructor(public name: string, public amount: number) {
   //it is also modal just like recipe.modal but created using different process
   //wherein we have argument and its type inside the constructor
  }

}
